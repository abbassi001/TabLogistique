{% extends 'colis_wizard/layout.html.twig' %}

{% block step_title %}Étape 4 : Statut initial du colis{% endblock %}

{% block step_content %}
    <p class="text-muted mb-4">
        Définissez le statut initial de votre colis. Par défaut, nous suggérons "Reçu" pour les colis nouvellement enregistrés.
    </p>

    {{ form_start(form, {'attr': {'id': 'wizard_form', 'class': 'needs-validation'}}) }}
        <div class="row">
            <div class="col-md-6">
                <div class="mb-3">
                    {{ form_label(form.type_statut) }}
                    {{ form_widget(form.type_statut, {'attr': {'class': 'form-select'}}) }}
                    {{ form_errors(form.type_statut) }}
                    <div class="form-text">État actuel du colis dans le processus d'acheminement.</div>
                </div>
                
                <div class="mb-3">
                    {{ form_label(form.date_statut) }}
                    {{ form_widget(form.date_statut, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.date_statut) }}
                </div>
            </div>
            
            <div class="col-md-6">
                <div class="mb-3">
                    {{ form_label(form.localisation) }}
                    {{ form_widget(form.localisation, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.localisation) }}
                    <div class="form-text">Emplacement actuel du colis (entrepôt, ville, pays, etc.)</div>
                </div>
                
                <div class="mb-3">
                    {{ form_label(form.employe) }}
                    {{ form_widget(form.employe, {'attr': {'class': 'form-select'}}) }}
                    {{ form_errors(form.employe) }}
                    <div class="form-text">Employé responsable du traitement de ce colis (facultatif).</div>
                </div>
            </div>
        </div>
        
        <div class="alert alert-info mt-3">
            <i class="fas fa-info-circle me-2"></i>
            Ce statut sera enregistré comme le premier événement dans l'historique de suivi du colis.
        </div>
    {{ form_end(form) }}
    {# Ajout de votre fichier JavaScript de wizard #}
{% block javascripts %}
    {# JavaScript de base #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    
    {# Votre fichier wizard.js #}
    <script src="{{ asset('javascript/wizard.js') }}"></script>
    
    {# Scripts additionnels spécifiques à chaque page #}
    {% block page_scripts %}{% endblock %}
{% endblock %}
{% endblock %}