{% extends 'colis_wizard/layout.html.twig' %}

{% block step_title %}Étape 5 : Informations de transport{% endblock %}

{% block step_content %}
    <p class="text-muted mb-4">
        Définissez les informations de transport pour ce colis. Si vous ne les connaissez pas encore, vous pourrez les ajouter ultérieurement.
    </p>

    {{ form_start(form, {'attr': {'id': 'wizard_form', 'class': 'needs-validation'}}) }}
        <div class="row">
            <div class="col-md-6">
                <div class="mb-3">
                    {{ form_label(form.type_transport) }}
                    {{ form_widget(form.type_transport, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.type_transport) }}
                    <div class="form-text">Type de transport (aérien, maritime, routier, etc.)</div>
                </div>
                
                <div class="mb-3">
                    {{ form_label(form.compagnie_transport) }}
                    {{ form_widget(form.compagnie_transport, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.compagnie_transport) }}
                </div>
                
                <div class="mb-3">
                    {{ form_label(form.numero_vol_navire) }}
                    {{ form_widget(form.numero_vol_navire, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.numero_vol_navire) }}
                    <div class="form-text">Numéro de vol, de navire ou identifiant du transport.</div>
                </div>
            </div>
            
            <div class="col-md-6">
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            {{ form_label(form.date_depart) }}
                            {{ form_widget(form.date_depart, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.date_depart) }}
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="mb-3">
                            {{ form_label(form.lieu_depart) }}
                            {{ form_widget(form.lieu_depart, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.lieu_depart) }}
                        </div>
                    </div>
                </div>
                
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            {{ form_label(form.date_arrivee) }}
                            {{ form_widget(form.date_arrivee, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.date_arrivee) }}
                        </div>
                    </div>
                    
                    <div class="col-md-6">
                        <div class="mb-3">
                            {{ form_label(form.lieu_arrivee) }}
                            {{ form_widget(form.lieu_arrivee, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.lieu_arrivee) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="alert alert-info mt-3">
            <i class="fas fa-info-circle me-2"></i>
            Ces informations permettent de suivre l'acheminement prévu du colis.
        </div>
    {{ form_end(form) }}
{% endblock %}

{% block navigation_buttons %}
    {% if current_step > 1 %}
        <a href="{{ path('app_colis_wizard_step' ~ (current_step - 1)) }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left me-2"></i>Précédent
        </a>
    {% else %}
        <a href="{{ path('app_colis_index') }}" class="btn btn-outline-secondary">
            <i class="fas fa-times me-2"></i>Annuler
        </a>
    {% endif %}
    
    <a href="{{ path('app_colis_wizard_cancel') }}" class="btn btn-outline-danger">
        <i class="fas fa-trash me-2"></i>Annuler la création
    </a>
    
    <div class="btn-group">
        <button type="submit" form="wizard_form" class="btn btn-primary">
            Suivant<i class="fas fa-arrow-right ms-2"></i>
        </button>
        <a href="{{ path('app_colis_wizard_step6') }}" class="btn btn-outline-primary">
            Ignorer cette étape
        </a>
    </div>
{% endblock %}